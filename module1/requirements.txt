# Module 1 — Data Ingestion + Entity Split dependencies
requests>=2.31.0

# Entity split (module1/entity_split.py) — also needed by Module 2 and Module 3b
networkx>=3.2.0       # connected-component entity proxy (METHODOLOGY.md §1)
scikit-learn>=1.4.0   # stratified entity-level train/val/test split

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0